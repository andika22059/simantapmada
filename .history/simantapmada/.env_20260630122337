# Alamat backend (origin saja, TANPA /api dan TANPA slash di akhir).
# Dipakai untuk semua request API & URL file (foto aset, file arsip).
# Untuk lokal biarkan seperti ini. Saat hosting, cukup ganti baris ini
# di .env.production (atau di sini) menjadi domain backend kamu.
VITE_API_URL=https://api.mantapmada.com
