# Dipakai otomatis saat `npm run build` (mode production).
# GANTI nilai di bawah ke domain backend hosting kamu (origin saja, tanpa /api,
# tanpa slash di akhir). Contoh: https://api.simantapmada.my.id
VITE_API_URL=https://api.mantapmada.com
